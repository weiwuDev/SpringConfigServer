spring:
  cloud:
    config:
      server:
        git:
          uri: ${config_git_uri}
encrypt:
  key-store:
    location: classpath:server.jks
    alias: ${key_alias}
    password: ${store_pass}
    secret: ${key_secret}
